# Required metadata
sonar.projectKey=org.sonarqube:Cache-demo
sonar.projectName=Cache-demo
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files


